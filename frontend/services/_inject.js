'use strict';

if (typeof module !== 'undefined' && typeof exports !== 'undefined' && module.exports === exports) {
    module.exports = 'app.services';
}

(function (angular) {
    var services = angular.module('app.services', []);
    // services.factory('apiFactory', require('./f.api'));
    // services.factory('wizardFactory', require('./f.wizard'));
    // services.factory('utilityCoordinateFactory', require('./f.utilityCoordinate'));
    // services.service('fabricInitService', require('./s.fabricInit'));
    // services.service('fabricService', require('./s.fabric'));
})(angular);