'use strict';

if (typeof module !== 'undefined' && typeof exports !== 'undefined' && module.exports === exports) {
    module.exports = 'app.directives';
}

(function (angular) {
    var directives = angular.module('app.directives', []);
    // directives.directive('fabric', require('./dir.fabric'));
})(angular);
